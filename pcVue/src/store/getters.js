const getters = {
  // isLoading: (state) => {
  //   return state.app.loadNum > 0
  // },
  // token: (state) => state.app.token,
  // isFolded: (state) => state.app.isFolded,
}
export default getters
